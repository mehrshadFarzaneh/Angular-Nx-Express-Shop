/**
 * Adapted from the original ng-packagr source.
 *
 * Changes made:
 * - Use custom cacheCompilerHost instead of the one provided by ng-packagr.
 * - Support ngcc for Angular < 16.
 * - Support Angular Compiler `incrementalDriver` for Angular < 16.
 */
import type { CompilerOptions, ParsedConfiguration } from '@angular/compiler-cli';
import { BuildGraph } from 'ng-packagr/lib/graph/build-graph';
import * as ts from 'typescript';
import { StylesheetProcessor } from '../styles/stylesheet-processor';
export declare function compileSourceFiles(graph: BuildGraph, tsConfig: ParsedConfiguration, moduleResolutionCache: ts.ModuleResolutionCache, extraOptions?: Partial<CompilerOptions>, stylesheetProcessor?: StylesheetProcessor, ngccProcessor?: any, watch?: boolean): Promise<void>;
