import { HostUrlChangerPipe } from './host-url-changer.pipe';

describe('HostUrlChangerPipe', () => {
  it('create an instance', () => {
    const pipe = new HostUrlChangerPipe();
    expect(pipe).toBeTruthy();
  });
});
